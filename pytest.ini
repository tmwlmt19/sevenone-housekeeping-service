[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
filterwarnings =
    # python-jose uses datetime.utcnow() internally; not our code
    ignore:datetime.datetime.utcnow:DeprecationWarning:jose.jwt
